# Backend host URL. Leave empty in development (the Vite proxy is used).
# In production set the Railway host ONLY — do NOT append /api:
# VITE_API_URL=https://assetsform-production.up.railway.app
VITE_API_URL=
